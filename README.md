cd ./server/
uv sync          // pip install uv  (if you don't have it)
                 // if you have difficulty installing uv visit 
                 //https://docs.astral.sh/uv/getting-started/installation/

uv run uvicorn app:app --port 8000

ctrl + leftclick on the link below!!  (same as opening in browser)
 
http://localhost:8000/static/index.html